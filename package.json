{
  "name": "musubi-s3",
  "version": "0.1.0",
  "description": "A lightweight S3-compatible server for learning purposes",
  "module": "src/main.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/main.ts",
    "dev": "bun --watch run src/main.ts",
    "test": "bun test",
    "typecheck": "bun run tsc --noEmit",
    "lint": "bun run tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.0"
  },
  "peerDependencies": {
    "typescript": "^5.8.0"
  }
}